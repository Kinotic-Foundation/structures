apiVersion: 1
datasources:
  - name: Loki
    type: loki
    uid: Loki
    url: http://loki:3100
    access: proxy
    isDefault: false
    jsonData:
      # Loki runs multi-tenant; browse the system tenant by default. Multi-tenant
      # queries accept pipe-separated ids (e.g. "acme|kinotic-system").
      httpHeaderName1: X-Scope-OrgID
      derivedFields:
        # OTLP log records carry the trace context as structured metadata, not in the log
        # line, so this matches the trace_id key rather than the body text.
        - name: TraceID
          datasourceUid: Tempo
          matcherType: label
          matcherRegex: trace_id
          url: "$${__value.raw}"
    secureJsonData:
      httpHeaderValue1: kinotic-system
  - name: Mimir
    type: prometheus
    uid: Mimir
    url: http://mimir:9009/prometheus
    access: proxy
    isDefault: true
    jsonData:
      httpMethod: POST
      prometheusType: Mimir
      # Mimir runs multi-tenant; browse the platform tenant by default, like Loki
      httpHeaderName1: X-Scope-OrgID
      # Matches OTEL_METRIC_EXPORT_INTERVAL on the server, so $__rate_interval resolves to a
      # window that actually contains samples.
      timeInterval: "15s"
      exemplarTraceIdDestinations:
        - name: trace_id
          datasourceUid: Tempo
          urlDisplayLabel: "View Trace"
    secureJsonData:
      httpHeaderValue1: kinotic-system
  - name: Tempo
    type: tempo
    uid: Tempo
    url: http://tempo:3200
    access: proxy
    isDefault: false
    jsonData:
      httpMethod: GET
      serviceMap:
        datasourceUid: Mimir
      nodeGraph:
        enabled: true
      tracesToLogsV2:
        datasourceUid: Loki
        # Widen the log window around the span on both sides — a start shift has to be
        # negative to search backwards.
        spanStartTimeShift: "-1h"
        spanEndTimeShift: "1h"
        filterByTraceID: true
        tags: [{ key: "service.name", value: "service_name" }]
      tracesToMetrics:
        datasourceUid: Mimir
        # service.name is the `service` label on the metrics-generator's span metrics.
        tags: [{ key: "service.name", value: "service" }]
        queries:
          - name: "Request rate"
            query: "sum(rate(traces_spanmetrics_calls_total{$$__tags}[$$__rate_interval]))"
          - name: "p95 latency"
            query: "histogram_quantile(0.95, sum by (le) (rate(traces_spanmetrics_latency_bucket{$$__tags}[$$__rate_interval])))"
      # Tempo runs multi-tenant; browse the platform tenant by default, like Loki
      httpHeaderName1: X-Scope-OrgID
    secureJsonData:
      httpHeaderValue1: kinotic-system
