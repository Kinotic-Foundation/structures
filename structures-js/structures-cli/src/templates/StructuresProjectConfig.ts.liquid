{% comment %}
  Liquid template for rendering a StructuresProjectConfig as TypeScript code.
  Only TypeScript projects are supported.
  Usage: Render with context { config, typeName }
{% endcomment %}

import type { {{ typeName }} } from '@kinotic/structures-api'

const config: {{ typeName }} = {% render_value config %}

export default config
